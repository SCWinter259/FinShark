import Hero from "../../Components/Hero/Hero.tsx";

interface Props {};

export const HomePage = ({}: Props) => {
    return (
        <div>
            <Hero/>
        </div>
    );
};

export default HomePage;
